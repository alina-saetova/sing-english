<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/navigation_favourites"
    app:startDestination="@id/favourites">

    <fragment
        android:id="@+id/favourites"
        android:name="ru.itis.sing_english.presentation.view.ui.FavouritesFragment"
        android:label="Favourites">
        <action
            android:id="@+id/action_favourites_to_graph_song"
            app:destination="@id/graph_song" />
    </fragment>
    <include app:graph="@navigation/navigation_song" />
</navigation>
